language: python
python:
  - "2.6"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: 
  - chmod +x ./build.sh
  - ./build.sh
branches:
  only:
    - raw
